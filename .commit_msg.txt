fix: live-screen org-id no longer breaks on Windows path separator

`adhoc:gc_strategies` was being interpreted as a colon-prefixed Windows
ADS, dropping the JSON cache file. Switched to `adhoc-<slug>` with
alphanum-only slugification so live search works end-to-end on Windows.
